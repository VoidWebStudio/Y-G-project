

var options = {
    strings: [ 'branding'], 
    typeSpeed: 90,
    backSpeed: 80,
    loop:true,
    cursorChar: '|',
    backDelay: 5500,
    startDelay: 1000,

  };
  
  var typed = new Typed('.element', options);

var type = {
    strings: [ 'Yudaev'], 
    typeSpeed: 90,
    backSpeed: 80,
    loop:false,
    cursorChar: '',
    backDelay: 5500,
    startDelay: 1000,

  };
  
  var typed = new Typed('.typed', type);

var type2 = {
    strings: [ 'Geravin'], 
    typeSpeed: 90,
    backSpeed: 80,
    loop:false,
    cursorChar: '|',
    backDelay: 5500,
    startDelay: 1000,

  };
  
  var typed = new Typed('.typed2', type2);


  new WOW().init();